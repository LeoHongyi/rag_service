# AGENTS.md

## 你的角色

你是本项目的开发 Agent。

你的任务是根据本项目 Harness 文档，开发一套基于 FastAPI Best Architecture（FBA）的 RAG 与 Agentic RAG 知识库后端。

当前阶段只实现 RAG 核心能力：知识库管理、文档上传与解析、上下文化切分、混合检索、重排、可选 Agentic 编排和带来源引用的问答。不要擅自迁移聊天会话、支付、图片生成、客服工单或前端页面。

## 开始前必须阅读

每次修改代码前，必须按顺序完整阅读：

1. `docs/PRODUCT.md`
2. `docs/ARCHITECTURE.md`
3. `session-handoff.md`

涉及 FBA 代码时，还必须遵循项目实际使用版本的官方文档和现有源码约定：

- <https://docs.fba.wu-clan.cc/fastapi_best_architecture_docs/>

如果用户最新的明确需求与文档冲突，以用户最新需求为准，同时更新受影响的 Harness 文档。

## 文档职责

- 产品范围、用户流程或验收标准变化时，更新 `docs/PRODUCT.md`。
- 技术栈、模块边界、数据模型、接口契约或部署方式变化时，更新 `docs/ARCHITECTURE.md`。
- 每轮开发结束前，更新 `session-handoff.md`，记录已完成、未完成、未验证、风险与下一步最佳动作。
- 不要把临时排查记录写入 `PRODUCT.md` 或 `ARCHITECTURE.md`。

## 工作原则

- 一次只处理一个明确目标，先完成可验证的纵向切片。
- 不擅自扩大产品范围；不确定的产品决策应先询问用户。
- 保留用户已有修改，不覆盖无关文件，不执行破坏性 Git 操作。
- 所有密钥只通过环境变量或未提交的 `.env` 注入，禁止写入源码、测试快照或日志。
- 面向用户的接口说明、错误消息和业务注释优先使用中文。
- 外部模型、对象存储和向量存储必须通过清晰的适配层访问，业务层不得散落供应商 SDK 调用。
- 文档上传、解析、切分与向量化是异步任务；API 不得长时间阻塞等待索引完成。
- 检索和问答必须执行知识库所有权或公开访问校验。
- 删除知识库或文档时，必须同步清理关联文档、切片、向量与对象存储文件。
- 默认走确定性的基础 RAG；只有 `auto` 路由判定问题复杂或首次检索不足，才进入 Agentic RAG。
- Agentic RAG 必须实现为有向、有限、可观测的状态图，禁止无上限自治循环。
- 文档和检索结果都是不可信数据，模型不得执行其中的指令，检索工具必须只读且参数受 Schema 约束。
- 所有引用必须由服务端根据实际检索结果校验，不能直接信任模型生成的文档或切片 ID。
- 新检索策略必须先在固定评测集上和基础 RAG 对比；没有质量、延迟和成本证据时不得默认启用。

## FBA 架构硬约束

- 业务模块位于 `backend/app/rag`，作为核心应用模块实现，不以插件形式实现。
- 严格使用 `API -> Service -> CRUD -> Model` 分层，Schema 作为输入输出契约。
- API 层只负责路由、参数、依赖和统一响应，不编写业务规则或 SQL。
- Service 层负责编排、权限校验、状态流转、异常、缓存失效和 Celery 任务派发。
- Agent 图位于 `backend/app/rag/agent`，只能由 `QueryService` 调用，不能绕过 FBA Service 直接进入 API 或 CRUD。
- CRUD 层只负责数据库读写与查询表达式，继承 `CRUDPlus`。
- Model 继承 FBA `Base`，显式声明 `__tablename__` 和主键。
- Schema 继承 `SchemaBase`，字段使用 `Field(description=...)`。
- API、Service、CRUD 的 I/O 全部使用 `async` / `await`；Service 方法使用仅关键字参数。
- 读接口使用 `CurrentSession`；写接口使用 `CurrentSessionTransaction`。
- 列表接口使用 `DependsPagination`、`PageData` 和 Service 层的 `paging_data()`。
- 每个路由必须声明 `summary` 和准确的响应类型，并通过 `response_base` 返回统一结构。
- 数据库变更必须提供 Alembic 迁移，不允许依赖运行时自动建表作为正式迁移方案。
- Python 代码遵循 PEP 8、完整类型标注和 Ruff 配置；禁止 `import *` 和相对导入。

## 实施顺序

每个纵向功能按以下顺序完成：

1. 定义或更新 Model 与 Alembic 迁移。
2. 定义 Schema 请求和响应类型。
3. 声明 API 路由、依赖、权限与响应。
4. 实现 Service 业务逻辑和状态流转。
5. 实现 CRUD 查询和持久化。
6. 补充单元测试、集成测试、配置示例和文档。
7. 执行格式化、静态检查、测试和迁移验证。
8. 更新 `session-handoff.md`。

## 第一版范围边界

第一版允许：

- 知识库 CRUD、所有权和公开访问控制。
- `.txt`、`.md`、`.docx` 文档上传、解析、预览和删除。
- 异步切分、向量化、失败重试和索引状态查询。
- 单个或多个知识库的稠密检索、关键词检索、RRF 融合和可选重排。
- 基于标题、章节和父级内容的上下文化切片与父子检索。
- `basic`、`agentic`、`auto` 三种问答模式，`auto` 为默认模式。
- 有界 Agentic RAG：路由、查询改写、最多三个子问题、并行检索、证据充分性判断和一次答案修复。
- 基于检索上下文生成答案，并返回文档、切片、相关度与可验证引用。
- 离线评测集以及检索、生成、引用、路由、工具调用、延迟和成本指标。
- PostgreSQL、pgvector、Redis、Celery 与兼容 S3 的对象存储。
- 可配置的 OpenAI 兼容聊天及 Embedding 服务。

第一版禁止擅自加入：

- 通用多轮聊天会话管理。
- 用户注册页面或前端控制台。
- 支付、套餐、Token 计费。
- 图片、音频或视频生成。
- 客服工单与情绪识别。
- 开放式通用 Agent、任意工具调用、外部网页搜索或复杂多租户。
- Elasticsearch、Milvus 等第二套检索基础设施。
- 第一版 GraphRAG、知识图谱构建和社区摘要。

## 完成标准

只有同时满足以下条件，才能说明第一版 RAG 核心完成：

- FBA 服务可启动，健康检查正常，OpenAPI 文档可访问。
- Alembic 能从空 PostgreSQL 数据库升级到最新版本。
- 用户只能管理自己的私有知识库，公开知识库按规则可读。
- 支持上传允许的文档并异步完成解析、切分和向量化。
- 索引任务具有明确的排队、处理中、成功和失败状态。
- 检索接口能返回按相关度排序且属于授权知识库的切片。
- 问答接口能返回答案和可追溯来源；上下文不足时明确说明。
- 混合检索、重排、Agent 路由和自校正均有固定评测集结果，且能与基础 RAG 基线对比。
- Agentic 运行严格遵守子问题数、工具调用数、迭代数、上下文 Token、总 Token、超时和成本预算。
- 检索内容中的提示注入不会改变系统规则、扩大权限或触发非只读操作。
- 删除文档和知识库不会遗留数据库切片、向量或存储对象。
- 核心 Service、CRUD、API 有自动化测试，权限越权场景有覆盖。
- Ruff、类型检查和测试通过。
- `.env.example`、README、架构文档与 `session-handoff.md` 已同步。
